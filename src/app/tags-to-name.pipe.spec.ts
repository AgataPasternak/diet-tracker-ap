import { TagsToNamePipe } from './tags-to-name.pipe';

describe('TagsToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TagsToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
